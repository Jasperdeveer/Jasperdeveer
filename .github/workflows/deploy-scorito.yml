name: Deploy Scorito Webapp

on:
  push:
    branches: [main]
    paths: [scorito-webapp/**, koyeb.yaml]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Koyeb deploys automatisch via GitHub-integratie
        run: echo "Koyeb picks up this push automatically"
